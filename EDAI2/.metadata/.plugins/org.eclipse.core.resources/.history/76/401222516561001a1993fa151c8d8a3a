package org.eda2;

public class Damero {
	private int filas;
	private int columnas; 
	private ParCalles pCalles;
	
	
	public Damero(int filas, int columnas) {
		this.filas = filas;
		this.columnas = columnas;
		
		if (filas%2==0) pCalles = new ParCalles[i/2][j]; //i vale la mitad porque unimos dos columnas
		else pCalles = new ParCalles[(i+1)/2][j];
		for(int a=0; a<pCalles.length; a++) {
			for(int b=0; b<pCalles[a].length; b++) {
				pCalles[a][b] = new ParCalles();
			}
		}
		
	}
	
	
	public void inicializarContadores() {
		
	}
	
	
}
